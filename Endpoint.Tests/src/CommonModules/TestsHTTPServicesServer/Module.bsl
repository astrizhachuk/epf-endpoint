#Область СлужебныйПрограммныйИнтерфейс

// @unit-test
Процедура SetBodyAsJSON(Фреймворк) Экспорт
	
	// given
	Результат = Новый HTTPСервисОтвет(200);
	Структура = Новый Структура();
	Структура.Вставить("Ключ1", 1);
	
	// when
	HTTPServices.SetBodyAsJSON(Результат, Структура);
	
	// then
	Фреймворк.ПроверитьВхождение(Результат.ПолучитьТелоКакСтроку(), """Ключ1"":1"); 
	
КонецПроцедуры

#КонецОбласти